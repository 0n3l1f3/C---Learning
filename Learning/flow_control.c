//all similar to c# flow control
#include <stdio.h>
#include <stdlib.h>

/*
int main(void)
{
    int r;

    do {
        r = rand() % 100;
        printf("%d\n", r);
    } while (r != 37); // Example condition, replace with actual logic
    
}
    */

    /*
int main(void)
{
    int i = 0;
    while (i < 10) {
        printf(" i is %d\n", i);
        i++;
    }
    
    for( i = 0; i<10; i++)
    {
        printf(" i is %d\n", i);
    }

}
    */
// switch sismilar to if but only works with integers and characters ( they are secret integers) and it is more efficient than if when you have many case
